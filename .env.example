# Copy this file to .env and fill in your values.
GEMINI_API_KEY=
# Optional: Google Gen AI SDK supports GOOGLE_API_KEY too.
GOOGLE_API_KEY=
# Optional: override model (default: gemini-2.5-flash).
# Use gemini-3-flash-preview or gemini-3-flash if your API access includes it.
GEMINI_MODEL=gemini-2.5-flash
# Optional: override API version (default: SDK beta endpoints).
# Example: v1 for stable endpoints, v1alpha for preview features.
GEMINI_API_VERSION=
