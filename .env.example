# Copy this file to .env and fill in your values.
GEMINI_API_KEY=
# Optional: override model (default: gemini-1.5-flash)
# Use gemini-3-flash if your API access includes it.
GEMINI_MODEL=gemini-1.5-flash
# Optional: override API version (default: v1 then v1beta fallback)
GEMINI_API_VERSION=
